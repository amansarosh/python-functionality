coordinates = (4, 5), (6, 7), (80, 84)  # tuples are like a coordinate. CANNOT BE CHANGED
print(coordinates[0])  # in 4, 5. 4 starts at 0 and 5 starts at 1

# lists use "[]" tuples use "()" lists can be changed; tuples cannot be.
#  tuples are usually used for data that wont be changed.
